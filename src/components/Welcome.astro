---
import { Branding } from "./auth/Branding";
import { GoogleSignInButton } from "./auth/GoogleSignInButton";
import { LegalLinks } from "./auth/LegalLinks";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "./ui/card";
---

<div class="flex min-h-screen flex-col">
  <main class="flex-1">
    <section class="container mx-auto px-4 py-12 sm:py-16 md:py-20">
      <div class="mx-auto max-w-4xl text-center space-y-8">
        <Branding client:load />
        <div class="space-y-4">
          <h1 class="text-3xl font-bold tracking-tight sm:text-4xl md:text-5xl text-foreground">
            Coordinate Your Family's Schedule
          </h1>
          <p class="text-lg sm:text-xl text-muted-foreground max-w-2xl mx-auto">
            A centralized calendar system that helps families manage children's activities, work commitments, and shared
            events—all in one place.
          </p>
        </div>
        <div class="flex justify-center pt-4">
          <div class="w-full max-w-[400px]">
            <GoogleSignInButton client:load />
          </div>
        </div>
      </div>
    </section>

    <section class="container mx-auto px-4 py-12 sm:py-16 bg-muted/30">
      <div class="mx-auto max-w-4xl space-y-8">
        <div class="text-center space-y-2">
          <h2 class="text-2xl font-semibold tracking-tight sm:text-3xl text-foreground">The Problem We Solve</h2>
          <p class="text-muted-foreground max-w-2xl mx-auto">
            Families struggle to coordinate complex schedules with multiple moving parts.
          </p>
        </div>

        <div class="grid gap-6 sm:grid-cols-2">
          <Card>
            <CardHeader>
              <CardTitle>Children's Activities</CardTitle>
              <CardDescription>
                Regular kindergarten schedules, extra lessons, and recurring activities create scheduling challenges.
              </CardDescription>
            </CardHeader>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>Adult Work Schedules</CardTitle>
              <CardDescription>
                Different work hours, fixed meetings, and flexible time slots make coordination difficult.
              </CardDescription>
            </CardHeader>
          </Card>

          <Card class="sm:col-span-2">
            <CardHeader>
              <CardTitle>Manual Calendar Management</CardTitle>
              <CardDescription>
                Paper calendars and separate digital calendars can't be easily shared, leading to conflicts and missed
                coordination.
              </CardDescription>
            </CardHeader>
          </Card>
        </div>
      </div>
    </section>

    <section class="container mx-auto px-4 py-12 sm:py-16">
      <div class="mx-auto max-w-4xl space-y-8">
        <div class="text-center space-y-2">
          <h2 class="text-2xl font-semibold tracking-tight sm:text-3xl text-foreground">Key Features</h2>
          <p class="text-muted-foreground max-w-2xl mx-auto">
            Everything you need to keep your family organized and synchronized.
          </p>
        </div>

        <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
          <Card>
            <CardHeader>
              <CardTitle>Multiple Calendar Views</CardTitle>
              <CardDescription>
                Day, Week, Month, and Agenda views optimized for desktop and mobile devices.
              </CardDescription>
            </CardHeader>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>Smart Conflict Detection</CardTitle>
              <CardDescription>
                Blocker events prevent scheduling conflicts, while Elastic events allow flexible planning.
              </CardDescription>
            </CardHeader>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>External Calendar Sync</CardTitle>
              <CardDescription>
                Connect Google Calendar and Microsoft 365 to see all your events in one place.
              </CardDescription>
            </CardHeader>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>Recurring Events</CardTitle>
              <CardDescription>
                Create daily, weekly, or monthly recurring events with automatic scheduling.
              </CardDescription>
            </CardHeader>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>Family Member Filtering</CardTitle>
              <CardDescription>
                Filter calendar views to see schedules for specific family members or children.
              </CardDescription>
            </CardHeader>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>Shared Family Calendar</CardTitle>
              <CardDescription>
                All family members can view and manage events together in a centralized system.
              </CardDescription>
            </CardHeader>
          </Card>
        </div>
      </div>
    </section>

    <section class="container mx-auto px-4 py-12 sm:py-16 bg-muted/30">
      <div class="mx-auto max-w-4xl space-y-8">
        <div class="text-center space-y-2">
          <h2 class="text-2xl font-semibold tracking-tight sm:text-3xl text-foreground">How It Works</h2>
        </div>

        <div class="grid gap-6 sm:grid-cols-3">
          <div class="text-center space-y-2">
            <div class="mx-auto size-12 rounded-full bg-primary/10 flex items-center justify-center">
              <span class="text-xl font-bold text-primary">1</span>
            </div>
            <h3 class="font-semibold text-lg">Sign In</h3>
            <p class="text-sm text-muted-foreground">Use your Google account to quickly get started.</p>
          </div>

          <div class="text-center space-y-2">
            <div class="mx-auto size-12 rounded-full bg-primary/10 flex items-center justify-center">
              <span class="text-xl font-bold text-primary">2</span>
            </div>
            <h3 class="font-semibold text-lg">Set Up Your Family</h3>
            <p class="text-sm text-muted-foreground">
              Create your family, add children, and invite members through our simple onboarding wizard.
            </p>
          </div>

          <div class="text-center space-y-2">
            <div class="mx-auto size-12 rounded-full bg-primary/10 flex items-center justify-center">
              <span class="text-xl font-bold text-primary">3</span>
            </div>
            <h3 class="font-semibold text-lg">Start Planning</h3>
            <p class="text-sm text-muted-foreground">
              Connect your calendars, create events, and coordinate your family's schedule.
            </p>
          </div>
        </div>
      </div>
    </section>

    <section class="container mx-auto px-4 py-12 sm:py-16">
      <div class="mx-auto max-w-2xl text-center space-y-8">
        <div class="space-y-4">
          <h2 class="text-2xl font-semibold tracking-tight sm:text-3xl text-foreground">Ready to Get Started?</h2>
          <p class="text-lg text-muted-foreground">
            Join families who are already using Home Planner to better coordinate their schedules and reduce conflicts.
          </p>
        </div>
        <div class="flex justify-center">
          <div class="w-full max-w-[400px]">
            <GoogleSignInButton client:load />
          </div>
        </div>
        <LegalLinks client:load />
      </div>
    </section>
  </main>
</div>
